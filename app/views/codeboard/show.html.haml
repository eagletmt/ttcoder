- content_for :title, 'Codeboard'
%h1= yield :title

%div
  %p The source code is refreshed every 5 seconds.
  %textarea#codeboard= @source

:javascript
  controlCodeboard('#{codeboard_path format: :json}', $('#codeboard'));
