namespace :deploy do
  desc 'Write REVISION file'
  task :write_revision do
    on release_roles(:all) do
      execute "echo #{fetch(:current_revision)} > #{current_path.join('REVISION')}"
    end
  end

  after :updated, :write_revision
end

# vim: set ft=ruby:
